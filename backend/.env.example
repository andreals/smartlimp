# Servidor
PORT=8080
ENV=development

# Banco de dados PostgreSQL (Neon)
DATABASE_URL=postgresql://test:test@test/test?channel_binding=require&sslmode=require

# Autenticação
JWT_SECRET=troque-este-segredo-em-producao
JWT_TTL_HOURS=12

# CORS - origem(s) permitidas separadas por vírgula
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
